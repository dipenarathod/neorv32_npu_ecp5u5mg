# Wishbone interconnect
- 1 Master in, 2 slaves out
- You can change the base address and mapping for the peripherals using a generic map
- I did not add support for a few pins. Specifically, the NPU pins
